mixin header-submenu(arg)
	ul.header-menu
		each item in arg
			li(class=(item.addClass ? ''+ item.addClass : ''))
				a(href=""+ item.link +".html") #{item.text}
				if item.sub
					.menu-drop
						each menusub in item.sub
							a(href=""+ menusub.link +".html") #{menusub.text}